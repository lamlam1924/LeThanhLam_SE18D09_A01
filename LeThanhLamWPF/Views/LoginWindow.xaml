<Window x:Class="LeThanhLamWPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeThanhLamWPF.Views"
        mc:Ignorable="d"
        Title="FU Mini Hotel System - Login" Height="500" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Background="#f0f0f0">
        <Border Background="White" CornerRadius="10" Margin="50" 
                BorderBrush="#ddd" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="5" Color="Gray" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel Margin="40" VerticalAlignment="Center">
                <TextBlock Text="FU Mini Hotel System" 
                          FontSize="28" FontWeight="Bold" 
                          HorizontalAlignment="Center" 
                          Foreground="#2c3e50" Margin="0,0,0,20"/>

                <TextBlock Text="Login to your account" 
                          FontSize="16" 
                          HorizontalAlignment="Center" 
                          Foreground="#7f8c8d" Margin="0,0,0,30"/>

                <Label Content="Email:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
                <TextBox x:Name="EmailTextBox" Height="40" Padding="15,10" FontSize="14"
                         BorderBrush="#bdc3c7" BorderThickness="2" Margin="0,0,0,15"/>

                <Label Content="Password:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="40" Padding="15,10" FontSize="14"
                            BorderBrush="#bdc3c7" BorderThickness="2" Margin="0,0,0,25"/>

                <Button Content="LOGIN" 
                        Click="LoginButton_Click"
                        Height="45" Margin="0,0,0,20" 
                        Background="#3498db" Foreground="White" 
                        FontSize="16" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="8" Padding="10">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2980b9"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#21618c"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Border BorderBrush="#ecf0f1" BorderThickness="1" Margin="0,10,0,15"/>

                <TextBlock Text="Test Accounts:" FontWeight="Bold" FontSize="12" 
                          HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Admin: admin@FUMiniHotelSystem.com" 
                          FontSize="11" Foreground="#7f8c8d" 
                          HorizontalAlignment="Center" Margin="0,0,0,3"/>
                <TextBlock Text="Password: @@abc123@@" 
                          FontSize="11" Foreground="#7f8c8d" 
                          HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Customer: john.doe@email.com" 
                          FontSize="11" Foreground="#7f8c8d" 
                          HorizontalAlignment="Center" Margin="0,0,0,3"/>
                <TextBlock Text="Password: password123" 
                          FontSize="11" Foreground="#7f8c8d" 
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>

</Window>
